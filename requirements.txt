# MediKiosk Backend — Python Dependencies
# Install: pip install -r requirements.txt

# === Core Backend ===
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv==1.1.0
python-multipart==0.0.20
pydantic==2.11.3

# === Database ===
aiosqlite==0.21.0

# === AI/ML Adapters ===
openai==1.82.0               # Groq + Ollama (OpenAI-compatible)
google-genai==1.14.0          # Gemini Flash (modern SDK)
onnxruntime==1.22.0           # CPU inference for embeddings, ASR, OCR
numpy==2.2.6

# === OCR Engine ===
rapidocr_onnxruntime==1.4.4
opencv-python-headless==4.11.0.86
Pillow==11.2.1

# === TTS Engine ===
# Cloud:   Sarvam AI Bulbul V3 (via httpx, no extra pip package)
# Offline: IndicF5 (AI4Bharat) runs on the GPU Edge Server, accessed via SPEECH_SERVICE_URL
#          Install on GPU server: pip install git+https://github.com/ai4bharat/IndicF5.git

# === ASR Engine ===
# Cloud:   Sarvam AI Saaras V4 (via httpx, no extra pip package)
# Offline: IndicConformer 600M (AI4Bharat) / faster-whisper run on the GPU Edge Server,
#          accessed via SPEECH_SERVICE_URL
#          Install on GPU server: pip install nemo_toolkit[asr] onnxruntime

# === Semantic Embedding (Concept Normalizer) ===
# paraphrase-multilingual-MiniLM-L12-v2 (~470 MB) for symptom-to-SNOMED/NAMASTE
# matching across hi, mr, ta, te, en. Never swap in an English-only model.
sentence-transformers>=3.0.0

# === Utilities ===
httpx==0.28.1
aiofiles==24.1.0
uuid7==0.1.0

# === Testing ===
pytest>=8.0.0
pytest-asyncio>=0.23.0
